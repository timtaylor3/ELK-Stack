output {
  if "cas" in [tags] {
    elasticsearch {
       hosts => "localhost:9200"
       index => "logstash-%{+YYYY.MM.dd}"
       #template => "/usr/local/elk/lib/elasticsearch-cas-template.json"
       #template_name => "filebeat-*"
       #template_overwrite => true
    }
  }
}
