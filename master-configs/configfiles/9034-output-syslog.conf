# Updates by Tim Taylor
# Change the grok-patterns directory
# Updated logic to use tags since type was no longer determined by configuration. 
# Commented out template until it can be updated to Version 6 specs 
output {
  if "syslog" in [tags] {
    elasticsearch {hosts => ["localhost:9200"]
       hosts => "localhost:9200"
       index => "logstash-%{+YYYY.MM.dd}"
       #template => "/usr/local/elk/lib/elasticsearch-logstash-template.json"
       #template_name => "logstash-*"
       #template_overwrite => true
       #document_type => "%{[@metadata][type]}"
    }
  }
}
